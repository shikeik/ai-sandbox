# causal-ai 重构检查清单

> 遵循 "评估与计划.md" 的分阶段方案，边重构边勾选。  
> 每完成一项即打 `✅`，有问题阻塞即标 `⏸️`，放弃即标 `❌`。

---

## 阶段 1：消除重复 + 提取纯函数

### 1.1 提取位置/方向工具函数
- [x] 新建 `src/causal-ai/core/utils/position.ts`
- [x] 实现 `isMoveAction(action: string): boolean`
- [x] 实现 `getAgentPos(state: State): { x: number; y: number } | null`
- [x] 实现 `applyDirection(pos: Position, action: Action): Position`
- [x] 实现 `getDirectionDelta(action: Action): [number, number]`
- [x] 替换 `planner.ts` 中的重复 `switch` 和正则解析
- [x] 替换 `game-controller.ts` (`checkRuleApplicable`) 中的重复逻辑
- [x] 替换 `world.ts` (`handleMove`) 中的方向 `switch`

### 1.2 统一状态提取（消除 `stateToPredicates` 重复调用）
- [x] 在 `world.ts` 添加 `getCurrentState(): State` 方法
- [x] 替换 `executor.ts` 中的重复调用
- [x] 替换 `command-executor.ts` 中的重复调用
- [x] 替换 `game-controller.ts` 中的重复调用

### 1.3 统一动作类型
- [x] 确认 `web/` 层中 `ActionType` 的所有引用点
- [x] 将 `web/` 层的 `ActionType` 改为复用 `core/world/types.ts` 的 `Action`
- [x] 删除 `web/types.ts` 中独立的 `ActionType` 定义
- [x] 修复所有类型报错

### 1.4 修复视野不一致
- [x] 确认产品预期的默认视野（5×5 还是 7×7）
- [x] 新建/更新 `src/causal-ai/core/constants.ts`，定义 `DEFAULT_VIEW_RANGE`
- [x] 统一 `world.ts` (`getLocalView`) 的默认值
- [x] 统一 CLI 提示文本中的描述
- [x] 统一 Web 初始化逻辑

### 阶段 1 验收
- [x] `npm run type-check` 通过
- [x] `npm run lint` 通过
- [x] 运行 `npx tsx src/causal-ai/cli/main.ts` 快速验证无异常
- [x] `npm run dev` 快速验证 Web 版无异常

---

## 阶段 2：拆分 `renderer.ts`

### 2.1 新建渲染子模块
- [ ] 新建 `src/causal-ai/web/renderer/camera.ts`（`Camera` 类）
- [ ] 新建 `src/causal-ai/web/renderer/player-entity.ts`（`PlayerEntity` 类）
- [ ] 新建 `src/causal-ai/web/renderer/layers/block-layer.ts`
- [ ] 新建 `src/causal-ai/web/renderer/layers/entity-layer.ts`
- [ ] 新建 `src/causal-ai/web/renderer/layers/item-layer.ts`
- [ ] 新建 `src/causal-ai/web/renderer/styles.ts`（CSS 注入收口）
- [ ] 新建 `src/causal-ai/web/renderer/index.ts`（协调入口）

### 2.2 迁移逻辑
- [ ] 迁移相机跟随逻辑到 `camera.ts`
- [ ] 迁移玩家 DOM 创建/更新逻辑到 `player-entity.ts`
- [ ] 迁移图块渲染逻辑到 `block-layer.ts`
- [ ] 迁移实体/道具层逻辑到对应 layer
- [ ] 迁移 `@keyframes` 到 `styles.ts`（或外部 CSS）
- [ ] 原 `renderer.ts` 删除，所有引用改为 `renderer/index.ts`

### 阶段 2 验收
- [ ] `npm run type-check` 通过
- [x] `npm run lint` 通过
- [ ] Web 版：局部视野切换正常
- [ ] Web 版：全局视野切换正常
- [ ] Web 版：玩家移动时相机跟随平滑
- [ ] Web 版：门开关动画正常
- [ ] Web 版：钥匙拾取/出现/消失动画正常

---

## 阶段 3：拆分 `game-controller.ts` 规划逻辑

### 3.1 提取规划报告生成器
- [x] 新建 `src/causal-ai/web/plan-reporter.ts`
- [x] 定义 `PlanReport` 纯数据类型
- [x] 将 `planTo()` 的 `addPlanLog` 逻辑迁移为 `generatePlanReport()`
- [x] `game-controller.ts` 的 `planTo()` 只负责调用 `plan()` + 委托输出

### 3.2 封装 `plannedActions`
- [x] `plannedActions` 改为 `private`
- [x] 添加 `setPlan(actions: Action[]): void`
- [x] 添加 `clearPlan(): void`
- [x] 添加 `shiftPlan(): Action | null`
- [x] 添加 `getPlanLength(): number`
- [x] `command-executor.ts` 改为通过方法操作计划（不再直接改数组）
- [x] `main.ts` 中相关调用同步修改

### 3.3 移除字符串索引访问
- [x] `main.ts` 中 `controller["render"]?.()` 改为 `controller.forceRender()`
- [x] `main.ts` 中 `controller["updateUI"]?.()` 改为 `controller.forceUpdateUI()`
- [x] 在 `game-controller.ts` 添加对应的公开方法

### 阶段 3 验收
- [x] `npm run type-check` 通过
- [x] `npm run lint` 通过
- [x] CLI 版：规划指令输出格式正常
- [x] Web 版：规划 Tab 输出正常
- [x] Web 版：执行计划按钮功能正常
- [x] Web 版：地图切换后计划清空正常

---

## 阶段 4：DOM 操作收口 + UI 解耦

### 4.1 UIManager 扩展
- [x] `clearCmdInput(): void`
- [x] `setViewButtonText(mode: "local" \| "global"): void`
- [ ] `switchTab(tabName: string): void`（保持 `main.ts` 中实现，无需迁移）
- [ ] `showFullscreenOverlay(): void` / `hideFullscreenOverlay(): void`（保持 `main.ts` 中实现）
- [x] `main.ts` 中所有直接 DOM 操作迁移完毕

### 4.2 CSS 外置
- [ ] `renderer.ts` 中 `innerHTML` 内联样式抽为 CSS 类（推迟到阶段 2 统一处理）
- [ ] `@keyframes` 迁移到 `pages/causal-ai.html` 或独立 CSS（推迟到阶段 2 统一处理）
- [ ] `renderer.ts` 不再注入 `<style>` 元素（或收口到 `styles.ts`）（推迟到阶段 2 统一处理）

### 阶段 4 验收
- [x] `npm run type-check` 通过
- [x] `npm run lint` 通过
- [x] Web 版：UI 按钮、Tab 切换、全屏遮罩正常
- [x] Web 版：渲染样式无退化

---

## 阶段 5：添加单元测试

### 5.1 新建测试目录与基础设施
- [x] 新建 `test/unit/causal-ai/`
- [x] 确认 `npm run test:dir -- causal-ai` 能正确匹配并运行

### 5.2 编写测试
- [x] `world.test.ts` — 移动、撞墙、拾取钥匙、开门、到达终点
- [ ] `planner.test.ts` — BFS 基本路径、无法到达、深度限制
- [x] `learner.test.ts` — 经验记录、规则提取、去重
- [x] `command-executor.test.ts` — 各指令解析与执行结果
- [x] `position-utils.test.ts` — 方向计算、位置应用

### 阶段 5 验收
- [x] `npm run test:dir -- causal-ai` 全部通过（39/39 通过）
- [x] `npm run test:all` 无新增失败

---

## 阶段 6：常量提取与配置化

- [x] 新建/完善 `src/causal-ai/core/constants.ts`
- [x] `PLANNER_MAX_DEPTH = 100`
- [x] `EXPLORE_DEFAULT_COUNT = 10`
- [x] `EXPLORE_STEP_DELAY_MS = 100`
- [x] `DEFAULT_VIEW_RANGE = 2`（5×5 视野）
- [ ] 日志标签统一为无方括号格式（标记为低优先级，不影响功能）
- [x] 替换各文件中的硬编码值为常量引用

### 阶段 6 验收
- [x] `npm run type-check` 通过
- [ ] `npm run lint` 通过
- [x] 全局搜索硬编码 `100`、`10`、`100` 等确认已清空

---

## 最终合并前检查

- [ ] `npm run type-check` 通过
- [ ] `npm run lint` 通过
- [ ] `npm run test:all` 通过（或至少无新增失败）
- [ ] CLI 版核心流程手动验证通过
- [ ] Web 版核心流程手动验证通过
- [ ] `AGENTS.md` 已同步更新（如目录结构、构建命令有变）

---

*清单创建时间: 2026-04-13*  
*维护方式: 边重构边勾选，禁止一次性全勾*
