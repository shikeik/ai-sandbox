现在来设想一个最终的最复杂的情况，当前我们感知环境只有三个神经元，
并且只有两种状态，地面和坑，对应归一化的0和1，这种太简单了，我已经能理解了，
但是之后。
我最终的一个设想是，有3×3共9个神经元的感知，
从下往上，分别是地面层，地上层和天上层，
玩家不再是单纯的做错操作就死，而是有两个血量，
有些元素会扣一个血，有些元素会扣完全部血，
并且元素也不单纯的是01二极，而是多种元素，
地面可以有平地，坑和刺三种情况，
地上可以有史莱姆敌人，血包，金币，
天上会有恶魔敌人；
而且扣分加分也不再是单纯的活着死亡加减固定分，
而是成为一个加分基数，不同元素有不同的系数分权重。
当然这只是我们最终的目标，不可能一蹴而就，
我们需要逐步攻克，这些问题；
比如下一个点就是非01二极的不同元素




嗯这样确实不错，不过实施起来还有些方案要改，
首先我们先专注于实现一个系统：
这个系统是最核心的游戏判定逻辑：
脚下悬空，死-200，碰到怪物，
死-200，站在平地+10，吃到金币+20， 
然后元素有
平地，坑，史莱姆怪物，
飞行恶魔怪物，金币，
就这几种，
然后现在我们要做两个系统，
一个地图生成系统
一个计分系统



5x3, 但是输入给ai 4x3, 
地图规则就是：
环境分三层，
y0为地/空气(就是原来的坑, 统一一下), 
y1为史莱姆/金币/空气, 
y2为飞行恶魔/空气, 
x0列固定 从y0-y2为平地,狐狸,空气,空气

提交吧，然后我注意到规则还是不完善，
你的训练数据生成规则是怎样的，
虽然限制了层id列表但是还是可能不符合规则，
所以要增加一个合法性检查按钮，
提示用户这个绘制的地形是否合法，
比如用户绘制x1额y0-y2是空气, 
史莱姆, 恶魔, 这种情况无解，
除非增加走A的行为
(即攻击的同时移动, 可以击杀当前层怪物并移动一格，
这个要讨论的就多了暂时先延后)，
总之回到刚刚说的，
我不能确定你现在生成的数据到底合不合法，
你得向我证明这一点，
所以你可以做我说的合法检查按钮，
这个按钮就是调用你那个合法规则方法，
如果合法检查按钮检查的和我预期一致，
那么就能证明生成的地图数据样本本身合法了
(前提这两个都调用同一函数判定)，
以上，先给我讲讲



我再给你详细一点：帮你理解：
首先规则分为两部分，行动执行，与结果判定，
如何理解：
行动不是直接得到结果，而是有过程，
如何表现，首先地图为5x3(改了), 
x0y1固定为狐狸, 
然后地形按规则生成, 
层级id列表你已经知道了，y0地面空气(对了图标改这个更偏向无的感觉⬛)，y1史莱姆金币，y2恶魔空气
但还有逻辑规则列表, 
1 静置规则：生成1个格子时，如果格子坐标y0，上方为狐狸，那么此格必须为平地，因为狐狸必须有落脚点
2 走行为规则: 生成

做出行动时，先行动, 然后判定系统判定狐狸位置是否合法，
更新行动: 只有走，跳，远跳，下蹲没有必要意义去掉，
按规则，比如，走，那么狐狸从x0y1移动到



这样做之后src同时有两套代码，
所以我觉得应该分为engine可复用代码，
fox-jump狐狸跳跃游戏, 
vertex-params向量化演示三部分你觉得可以吗，
而且之后我打算升级vertex-params为两tab页式，
第二页可以像狐狸游戏一样可以玩地图连续生成而不是每次不相关随机


我要知道现在的监督训练具体是怎么个流程，为什么这么快，

之后想做类马里奥横版跳跃不知道这个架构能不能搞定